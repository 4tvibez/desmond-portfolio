const id=+new URLSearchParams(location.search).get('id'),p=products.find(x=>x.id===id),root=document.querySelector('#detail');if(!p)root.innerHTML='<h1>Product not found</h1>';else root.innerHTML=`<section class="product-detail"><img src="${p.img}" alt="${p.name}"><div><small>${p.cat}</small><h1>${p.name}</h1><span class="rating">★ ${p.rating}</span><h2>GH₵${p.price.toFixed(2)}</h2><p>${p.desc}</p><div class="buy"><input id="qty" type="number" min="1" value="1"><button class="btn" id="add">Add to cart</button></div></div></section>`;document.querySelector('#add')?.addEventListener('click',()=>{let c=JSON.parse(localStorage.getItem('novacart-cart')||'[]'),q=Math.max(1,+document.querySelector('#qty').value),x=c.find(i=>i.id===id);x?x.qty+=q:c.push({id,qty:q});localStorage.setItem('novacart-cart',JSON.stringify(c));document.querySelector('#add').textContent='Added ✓'});