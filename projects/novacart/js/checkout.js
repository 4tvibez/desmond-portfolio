let cart=JSON.parse(localStorage.getItem('novacart-cart')||'[]');const total=cart.reduce((s,x)=>s+products.find(p=>p.id===x.id).price*x.qty,0);document.querySelector('#summary').innerHTML=cart.length?cart.map(x=>{let p=products.find(p=>p.id===x.id);return `<div class="summary-row"><span>${p.name} × ${x.qty}</span><b>GH₵${(p.price*x.qty).toFixed(2)}</b></div>`}).join(''):'<p>Your cart is empty.</p>';document.querySelector('#sumTotal').textContent=`GH₵${total.toFixed(2)}`;document.querySelector('#form').onsubmit=e=>{e.preventDefault();if(!cart.length)return alert('Your cart is empty.');alert('Order placed! Demo checkout — no real payment is processed.');localStorage.removeItem('novacart-cart');location.href='index.html'};